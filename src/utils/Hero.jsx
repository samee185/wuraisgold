export default function Hero({title}) {
  return (
    <main className="px-5 h-[50vh] flex justify-center items-center text-white bg-primary text-3xl md:text-[64px] font-[500]">
      {title}
    </main>
  );
}
